<h1>Editing Book</h1>
<!-- form_for -->
<%= form_for(@book) do |f| %>
	<h4>Title</h4>
    <%= f.text_field :title %>
    <h4>body</h4>
    <%= f.text_area :body %>
    <%= f.submit 'Update Book' %>
<% end %>

<p><%= link_to "show", book_path(@book) %>|<%= link_to "back", books_path %></p>